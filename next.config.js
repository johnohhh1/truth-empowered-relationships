/** @type {import('next').NextConfig} */
const nextConfig = {
  // experimental.serverActions is no longer needed in Next.js 14+
  // Server Actions are enabled by default
}

module.exports = nextConfig
